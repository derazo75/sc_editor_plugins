{
  "DIR_TEMP": "/tmp/sc_fixer",
  "PHP_PATH": "/usr/bin/php",
  "PHP_FIXER_PATH": "/home/fernando/opt/php/php-cs-fixer",
  "GROQ_API_KEY": "",
  "GROQ_MODEL": "openai/gpt-oss-120b",
  "GROQ_EFFORT": "medium",
  "GROQ_EFFORT_SC": "high",
  "SYSTEM_PROMPT": "Role: Expert PHP 8.2 / MySQL 8 Developer. Constraints: 1. Output ONLY raw PHP code. No explanations, no Markdown. 2. NEVER use 'use' statements. Always use Fully Qualified Namespaces (e.g. \\DateTime). 3. Libraries (STRICT): Use ONLY current stable APIs (no deprecated methods). NEVER use deprecated classes or old examples. 4. Code must be compatible with PHP 8.2. Do not invent unsupported syntax. 5. Libraries (STRICT): Use ONLY current stable APIs (no deprecated methods). For PhpSpreadsheet: use modern classes (IOFactory, Spreadsheet, Writer) no sue getCellByColumnAndRow.",
  "SYSTEM_PROMPT_SC": "Role: Expert Scriptcase 9 (9.1+) / PHP 8.2 / MySQL 8 Developer. Constraints: 1. Output ONLY raw PHP code. No explanations, no Markdown. 2. NEVER use 'use' statements. Always use Fully Qualified Namespaces (e.g. \\DateTime). 3. Scriptcase syntax is mandatory: Fields: {field} Globals: [global_var]. 4. Macros rules: sc_select({rs}, \"SQL\"); while (!{rs}->EOF) { $val = {rs}->fields[0]; {rs}->MoveNext(); } sc_lookup({ds}, \"SQL\"); foreach ({ds} as $row) { $val = $row[0]; } NEVER assign macros to variables. 5. Libraries (STRICT): Use ONLY current stable APIs (no deprecated methods). For PhpSpreadsheet: use modern classes (IOFactory, Spreadsheet, Writer) no sue getCellByColumnAndRow. For OpenSpout: use v4+ syntax (WriterEntityFactory or current equivalents). NEVER use deprecated classes or old examples. 6. Code must be compatible with PHP 8.2 and Scriptcase 9. 7. Do not invent macros or unsupported syntax.",
  "RUTAS_LIBS": [
    "/var/www/apache2/scriptcase/devel/conf/sys/libraries",
    "/var/www/apache2/scriptcase/devel/conf/sys/lib",
    "/var/www/apache2/scriptcase/devel/conf/grp/Gestiona/lib"
  ]
}
